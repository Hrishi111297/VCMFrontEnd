import { base_url } from "../../Utils/MetaURL";
export const fetchProfileImage = `${base_url}/profile/profile-picture/`;
export const uploadProfileImage = `${base_url}/profile/upload-profile/`;
export const getUSerDetails = `${base_url}/profile/profile-details`;
